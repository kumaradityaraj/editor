{
  "extends": "./tsconfig.json",

  "compilerOptions": {
    "types": [
      "jest",
      "@testing-library/jest-dom",
      "node",
    ],

    // Jest support for ECMAScript Modules (ESM) is considered experimental, override to commonjs just for testing 
    "module": "commonjs",

    // Tests can import JS or TS freely
    "allowJs": true,

    // Preserve JSX so test runner interprets it
    "jsx": "react-jsx",

    // No declaration output during tests
    "declaration": false,
    "emitDeclarationOnly": false,
    "noEmit": true,

    // Tests often work with dynamic/loosely typed objects
    "exactOptionalPropertyTypes": false,
  },
  "include": [
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/tests/**/*",
  ],
}